export type PublicNote = {
    createdAt: string,
    note: string;
    public: boolean;
    summary: string;
    tmdbId: string;
    updatedAt: string | null;
    userId: string;
    viewId: string;
}